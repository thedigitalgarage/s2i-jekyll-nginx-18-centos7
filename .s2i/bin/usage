#!/bin/bash -e
cat <<EOF
This is the jekyll-nginx-18-centos7 S2I image.  For more information, please
refer to https://github.com/mrjoshuap/s2i-jekyll-nginx-18-centos7

To build a new jekyll site image:

1. install S2I from https://github.com/openshift/source-to-image

2. perform a source to image build on your jekyll site source

  # s2i build git://<source code> jekyll-nginx-18-centos7 <application image>

3. run the resulting image

  # docker run <application image>

EOF
